/* 

    @STATUS: Simple Save To LocalStorage DataBase!
    @INFO: I think that what you see is USELESS, but you can upgrade that method to something BIG

*/

function save_to_db(name, input) {
    localStorage.setItem(nane, input)
}